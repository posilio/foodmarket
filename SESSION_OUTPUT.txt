SESSION NAME: FOOD-029 — Product reviews
DATE: 2026-03-16

FILES CHANGED:
- packages/backend/src/prisma/schema.prisma — Added Review model (id, productId, customerId, rating Int, body String?, createdAt, updatedAt, @@unique([productId,customerId]), @@index([productId])). Added `reviews Review[]` relation to Product and Customer models.
- packages/backend/src/app.ts — Imported reviewsRouter and mounted it at /api/v1.
- packages/shared/src/index.ts — Added Review and ReviewsResponse interfaces.
- packages/storefront/src/lib/api.ts — Added getReviews(slug) and submitReview(slug, rating, body, token). Added Review and ReviewsResponse imports.
- packages/storefront/src/types/index.ts — Added Review and ReviewsResponse interface definitions.
- packages/storefront/src/app/products/[slug]/page.tsx — Imported ReviewsSection and rendered <ReviewsSection slug={product.slug} /> below the two-column product block.
- docs/IMPLEMENTATION_BACKLOG.md — FOOD-029 status TODO → DONE in both ticket and summary table.

FILES CREATED:
- packages/backend/src/prisma/migrations/20260316180000_add_review/migration.sql — Creates Review table with PKconstraint, two FK constraints (→ Product, → Customer), unique index on (productId, customerId), and productId index.
- packages/backend/src/services/reviews.service.ts — getReviewsForProduct(slug): returns reviews array + averageRating + totalReviews. createReview(customerId, slug, rating, body): validates 1–5 rating, upserts (update if same customer+product already reviewed).
- packages/backend/src/routes/reviews.ts — GET /products/:slug/reviews (public). POST /products/:slug/reviews (requireAuth, 201).
- packages/storefront/src/components/ReviewsSection.tsx — 'use client' component. Loads reviews on mount via getReviews(). Shows average star rating + count in heading. Lists reviews (stars, firstName, date, body). Shows star-picker form + optional textarea for logged-in users; shows "Sign in" link for guests. Submits via submitReview(), shows success state, reloads reviews.

MIGRATIONS:
- 20260316180000_add_review — Adds Review table; one review per customer per product (unique constraint). Deployed to local DB via prisma migrate deploy.

ENDPOINTS ADDED/CHANGED:
- GET /api/v1/products/:slug/reviews — public → { reviews: [{id, rating, body, createdAt, customer:{firstName}}], averageRating: number|null, totalReviews: number }
- POST /api/v1/products/:slug/reviews — Authorization: Bearer <token> required; body: { rating: number (1-5), body?: string } → 201 { data: { id, rating, body, createdAt, updatedAt, customer:{firstName} } }

FRONTEND CHANGES:
- ReviewsSection (storefront) — new client component on product detail page
- ProductDetailPage (storefront) — added <ReviewsSection slug={product.slug} /> below product info

ERRORS/ISSUES ENCOUNTERED:
- storefront/src/types/index.ts is a local types file (not re-exporting from @foodmarket/shared). Added Review and ReviewsResponse directly there rather than importing from the shared package.

TEST RESULTS:
- Build: PASS (backend tsc --noEmit clean, storefront tsc --noEmit clean)
- Tests: 17 passed, 0 failed

NOTES FOR CLAUDE.AI:
- The Review model uses upsert so a customer can update their review (not create a second one). The POST endpoint returns 201 on both create and update.
- updatedAt in the migration SQL uses DEFAULT CURRENT_TIMESTAMP as a fallback; Prisma manages it programmatically on every update.
- Remaining backlog: FOOD-025 (order cancellation + Mollie refund), FOOD-030 (discount codes), FOOD-020/021 (go-live).
