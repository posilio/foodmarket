SESSION NAME: feat: FOOD-026 low stock alert job with node-cron + per-admin emails
DATE: 2026-03-18

FILES CHANGED:
- packages/backend/src/jobs/lowStockAlert.ts — full rewrite: switched from setInterval+hour-check to node-cron (daily 08:00 Amsterdam), queries admin Customer records instead of ADMIN_EMAIL env var, exported runLowStockAlert(), added top-level try/catch for graceful degradation
- packages/backend/src/lib/email.ts — sendLowStockAlert() now accepts (variants, to: string) instead of reading ADMIN_EMAIL from env; removed ADMIN_EMAIL import (no longer needed in this file)
- package-lock.json — node-cron added as dependency

FILES CREATED:
- None

MIGRATIONS:
- None

ENDPOINTS ADDED/CHANGED:
- None

FRONTEND CHANGES:
- None

ERRORS/ISSUES ENCOUNTERED:
- None

TEST RESULTS:
- Build: PASS (tsc --noEmit clean)
- Tests: 17 passed, 0 failed

NOTES FOR CLAUDE.AI:
- node-cron installed: "node-cron" in dependencies, "@types/node-cron" in devDependencies of packages/backend.
- sendLowStockAlert() signature changed — callers must now pass the recipient email address as second argument. Only caller is lowStockAlert.ts (loops over admin customers and calls once per admin).
- runLowStockAlert() is now exported — can be called directly in tests or one-off scripts.
- startLowStockAlertJob() schedules via cron.schedule('0 8 * * *', ..., { timezone: 'Europe/Amsterdam' }) — no more manual hour-of-day check needed.
- ADMIN_EMAIL env var is still defined in config/env.ts (kept for forward compat / Railway config) but is no longer read by the email module.
- If no admin customers exist in the DB, the job logs a warning and skips sending.
