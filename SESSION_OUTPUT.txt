SESSION NAME: perf: client-side product filtering
DATE: 2026-03-17

FILES CHANGED:
- packages/storefront/src/app/products/page.tsx — Simplified to a lean server component: fetches ALL products (no filter params) + getCategoryTree() once, reads only initialRegion and initialType from URL searchParams (for homepage links), passes everything to ProductsPageClient. Removed all filter/search/heading logic.
- packages/storefront/src/components/DualFilterPills.tsx — Replaced router.push with onChange callback props (onRegionChange, onCountryChange, onTypeChange). Removed useRouter and useSearchParams dependencies. Buttons now call callbacks directly for instant in-memory filtering.
- packages/storefront/src/components/SearchInput.tsx — Added optional onChange?: (q: string) => void prop. When provided: calls onChange with debounced value (200ms), skips all URL manipulation. URL mode (no onChange) unchanged for backward compat.

FILES CREATED:
- packages/storefront/src/components/ProductsPageClient.tsx — 'use client' component. Receives allProducts (Product[]) and categoryTree as props. Maintains activeRegion, activeCountry, activeType, q in useState. Filters products in-memory via useMemo: region filter uses category.parentId, country filter uses category.id, type filter uses typeCategory?.id, search is case-insensitive name+description match. Renders SearchInput (with onChange), DualFilterPills (with callbacks), heading, count, and product grid. Derives heading from active filter state.

MIGRATIONS: None

ENDPOINTS ADDED/CHANGED: None

FRONTEND CHANGES:
- ProductsPage (storefront) — now a thin server component; all UI and state moved to ProductsPageClient
- ProductsPageClient (storefront) — new client component, instant in-memory filtering with useState/useMemo
- DualFilterPills (storefront) — callback-based, no router dependency
- SearchInput (storefront) — optional onChange prop for callback mode

ERRORS/ISSUES ENCOUNTERED: None

TEST RESULTS:
- Build: PASS (storefront tsc --noEmit clean)
- Tests: not run (no backend changes)

NOTES FOR CLAUDE.AI:
- Homepage region/type links still use ?region= and ?type= URL params; the server component reads them as initialRegion/initialType and passes to the client. After initial render, all filtering is client-side with no URL changes.
- SearchInput in URL mode (no onChange prop) is unchanged — still works on any other page that uses it.
- If the product catalogue grows very large (500+), consider adding a virtualised list. For now 54 products filter instantly.
- Remaining backlog: FOOD-025 (order cancellation + Mollie refund), FOOD-030 (discount codes), FOOD-020/021 (go-live).
