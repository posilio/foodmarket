// Next.js configuration for the customer-facing storefront.
// Add image domains, rewrites, and other Next.js options here.

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
