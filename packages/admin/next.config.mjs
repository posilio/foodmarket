// Next.js configuration for the internal admin app.
// Add auth redirects, rewrites, and other admin-specific Next.js options here.

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
